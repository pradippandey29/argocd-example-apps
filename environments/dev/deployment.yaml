apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-dev
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: guestbook-dev
  template:
    metadata:
      labels:
        app: guestbook-dev
    spec:
      containers:
      - name: guestbook
        image: 961248554445.dkr.ecr.us-west-2.amazonaws.com/guestbook-app:v5
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: guestbook-dev-svc
  namespace: default
spec:
  selector:
    app: guestbook-dev
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP